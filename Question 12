# Q. 12 Write a program to create a series object from an nddarray that stores characters from a to g
import pandas as pd
import numpy as np
array=np.array(['a','b','c','d','e','f','g'])
s1=pd.Series(array)
print(s1)
